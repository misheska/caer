ADD_LIBRARY(accumulator SHARED accumulator.cpp)

SET_TARGET_PROPERTIES(accumulator
	PROPERTIES
	PREFIX "dv_"
)

TARGET_LINK_LIBRARIES(accumulator
	PRIVATE
		dvsdk
		${OpenCV_LIBS})

INSTALL(TARGETS accumulator DESTINATION ${DV_MODULES_DIR})
