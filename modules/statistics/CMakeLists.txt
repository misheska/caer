ADD_LIBRARY(statistics SHARED statistics.c)

SET_TARGET_PROPERTIES(statistics
	PROPERTIES
	PREFIX "dv_"
)

TARGET_LINK_LIBRARIES(statistics ${DV_LIBS})

INSTALL(TARGETS statistics DESTINATION ${DV_MODULES_DIR})
