file_identifier "MCTR";

namespace dv;

enum ModuleCommand : int8 {
	START = 0,
	SHUTDOWN = 1,
	RESTART = 2,
	TS_RESET = 3
}

table ModuleControl {
	cmd: ModuleCommand = START;
	id: uint64;
}

root_type ModuleControl;